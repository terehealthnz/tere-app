{
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/handler?route=$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "functions": {
    "api/handler.js": { "maxDuration": 30 }
  }
}
